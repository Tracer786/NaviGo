1. User Registration (POST)

curl --location 'http://localhost:4000/users/register' \
--header 'Content-Type: application/json' \
--data-raw '{
    "fullname": {
        "firstname" : "NaviGo",
        "lastname" : "App"
    },
    "email" : "navigo@gmail.com",
    "password" : "NaviGo"
}'

2. User Login (POST)

curl --location 'http://localhost:4000/users/login' \
--header 'Content-Type: application/json' \
--data-raw '{
    "email" : "navigo@gmail.com",
    "password" : "NaviGo"
}'

3. 


